# Executable
executable = /home/najroldi/cluster_101/condor/hello_condor.sh

# Logs
error = log/job.$(Cluster).$(Process).err
output = log/job.$(Cluster).$(Process).out
log = log/job.$(Cluster).$(Process).log

# Compute requests
request_memory = 1024
request_cpus = 1
request_gpus = 0

# Submit 1 job
queue